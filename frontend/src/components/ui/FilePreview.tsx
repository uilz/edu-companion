"use client";

/**
 * FilePreview – 预览按钮弹出卡片的预览组件
 *
 * 实际渲染逻辑委托给 PreviewContent，此处保留重新导出以保持向后兼容。
 */
export { default, getExt } from "@/components/PreviewContent";
export type { PreviewFile } from "@/components/PreviewContent";