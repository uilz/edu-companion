// ============================================================
// RightPanel — 右栏工作面板 (stub)
//
// 任务 #76 应有正式实现（AI 助手 / 任务面板 / 快速操作）；
// 本文件为占位，让 build 通过。
// ============================================================

"use client";

export default function RightPanel() {
  return (
    <div
      data-testid="right-panel"
      className="h-full w-full flex flex-col items-center justify-center text-[var(--color-text-muted)] text-xs p-3"
    >
      <p>右栏</p>
      <p className="text-[10px] opacity-60 mt-1">（任务 #76 正式版未实现）</p>
    </div>
  );
}
